<div class="card">
  <div class="card-body">
    <h6 class="card-subtitle mb-2 text-muted">
      <i class="{{icon}}"></i>
      {{title}}
    </h6>
    <h5 class="card-title" id="current-moisture-level">{{body}}</h5>
    <h7 class="card-subtitle mb-2 text-muted">{{subtitle}}</h7>
  </div>
</div>